filebeat:
  prospectors:
    -
      paths:
        - /var/log/metrics/*.log
      input_type: log
      document_type: metrics
      scan_frequency: 10s
output:
  logstash:
    hosts: ["logstash:5044"]
logging:
  files:
    rotateeverybytes: 10485760 # = 10MB
  selectors: ["*"]
  level: warning
